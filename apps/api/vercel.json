{
  "version": 2,
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "npm run build --prefix ../../packages/shared && npx prisma generate --schema=prisma/schema.prisma",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
