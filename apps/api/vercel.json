{
  "version": 2,
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "npx prisma generate --schema=prisma/schema.prisma",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api"
    }
  ]
}
